# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/

# Entorno
.env

# Datos (no se versionan; se descargan con scripts/download_data.sh)
data/raw/
data/processed/
*.parquet

# Resultados pesados (guarda solo gráficos finales y CSV resumen)
results/raw/

# SO / editor
.DS_Store
.idea/
.vscode/

# Artefactos de modelos: los del demo (codebooks + índices, ~2 MB) SÍ se versionan
# para que el proyecto sea reproducible al clonar (ver scripts/demo.sh).
# Si entrenas modelos pesados que NO quieras versionar, ignóralos puntualmente aquí.
