# Copia este archivo a .env y ajústalo. NO subas .env al repo.

# Opción A (desarrollo en local):  DB_HOST=localhost
# Opción B (todo en Docker):       DB_HOST=postgres
DB_HOST=localhost
DB_PORT=5432
DB_NAME=multimodal
DB_USER=postgres
DB_PASSWORD=postgres
